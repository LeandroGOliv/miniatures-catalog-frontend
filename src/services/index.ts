import { authService } from './auth.service'
import { miniaturesService } from './miniatures.service'

const services = {
  auth: authService,
  miniatures: miniaturesService,
}

export default services
